[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "check-printing"
version = "0.1.0"
description = "Generate print-ready duplex PDFs for compact personal check printing."
readme = "README.md"
requires-python = ">=3.12,<3.14"
license = { text = "MIT" }
authors = [{ name = "check_printing maintainers" }]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.12",
  "Topic :: Office/Business :: Financial",
]
dependencies = [
  "pydantic==2.8.2",
  "PyYAML==6.0.2",
  "reportlab==4.2.2",
  "typer==0.12.5",
  "click==8.1.7",
]

[project.optional-dependencies]
dev = [
  "mypy==1.11.2",
  "pytest==9.0.3",
  "pytest-cov==5.0.0",
  "ruff==0.6.3",
  "pre-commit==3.8.0",
  "types-PyYAML==6.0.12.20240808",
]
fonts = [
  "fonttools==4.53.1",
  "pillow==10.4.0",
]
ui = [
  "streamlit==1.38.0",
  "pymupdf==1.24.10",
]
api = [
  "fastapi==0.112.2",
  "uvicorn==0.30.6",
]

[project.scripts]
check-print = "check_printing.cli:app"

[tool.setuptools.packages.find]
include = ["check_printing*"]

[tool.setuptools.package-data]
check_printing = ["layout_templates.yaml"]

[tool.setuptools]
license-files = ["LICENSE", "THIRD_PARTY_NOTICES.md"]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["check_printing", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "C4", "SIM", "RUF"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"check_printing/cli.py" = ["B008", "UP007"]

[tool.mypy]
python_version = "3.12"
packages = ["check_printing"]
strict = true
warn_unreachable = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra --strict-markers --cov=check_printing --cov-report=term-missing"
testpaths = ["tests"]
